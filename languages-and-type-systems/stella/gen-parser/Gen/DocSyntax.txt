The Language Syntax
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of Syntax==

===Literals===
Integer literals //Integer// are nonempty sequences of digits.






StellaIdent literals are recognized by the regular expression
`````('_' | letter) (["!-:?_"] | digit | letter)*`````

ExtensionName literals are recognized by the regular expression
`````'#' (["-_"] | digit | letter)+`````

MemoryAddress literals are recognized by the regular expression
`````{"<0x"} (["ABCDEFabcdef"] | digit)+ '>'`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in Syntax are the following:
  | ``Bool`` | ``Bot`` | ``Nat`` | ``Top``
  | ``Unit`` | ``and`` | ``as`` | ``cast``
  | ``catch`` | ``cons`` | ``core`` | ``else``
  | ``exception`` | ``extend`` | ``false`` | ``fix``
  | ``fn`` | ``fold`` | ``forall`` | ``generic``
  | ``if`` | ``in`` | ``inl`` | ``inline``
  | ``inr`` | ``language`` | ``let`` | ``letrec``
  | ``match`` | ``new`` | ``not`` | ``or``
  | ``return`` | ``succ`` | ``then`` | ``throw``
  | ``throws`` | ``true`` | ``try`` | ``type``
  | ``unfold`` | ``unit`` | ``variant`` | ``with``
  | ``µ`` |  |  |

The symbols used in Syntax are the following:
  | , | ; | ( | )
  | { | } | [ | ]
  | = | : | -> | =>
  | | | <| | |> | :=
  | < | <= | > | >=
  | == | != | + | -
  | * | / | . | List::head
  | List::isempty | List::tail | panic! | Nat::pred
  | Nat::iszero | Nat::rec | & |

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of Syntax==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //LanguageDecl// //[Extension]// //[Decl]//
  | //[StellaIdent]// | -> | **eps**
  |  |  **|**  | //StellaIdent//
  |  |  **|**  | //StellaIdent// ``,`` //[StellaIdent]//
  | //LanguageDecl// | -> | ``language`` ``core`` ``;``
  | //Extension// | -> | ``extend`` ``with`` //[ExtensionName]//
  | //[ExtensionName]// | -> | **eps**
  |  |  **|**  | //ExtensionName//
  |  |  **|**  | //ExtensionName// ``,`` //[ExtensionName]//
  | //[Extension]// | -> | **eps**
  |  |  **|**  | //Extension// ``;`` //[Extension]//
  | //Decl// | -> | //[Annotation]// ``fn`` //StellaIdent// ``(`` //[ParamDecl]// ``)`` //ReturnType// //ThrowType// ``{`` //[Decl]// ``return`` //Expr// ``}``
  |  |  **|**  | //[Annotation]// ``generic`` ``fn`` //StellaIdent// ``[`` //[StellaIdent]// ``]`` ``(`` //[ParamDecl]// ``)`` //ReturnType// //ThrowType// ``{`` //[Decl]// ``return`` //Expr// ``}``
  |  |  **|**  | ``type`` //StellaIdent// ``=`` //Type//
  |  |  **|**  | ``exception`` ``type`` ``=`` //Type//
  |  |  **|**  | ``exception`` ``variant`` //StellaIdent// ``:`` //Type//
  | //[Decl]// | -> | **eps**
  |  |  **|**  | //Decl// //[Decl]//
  | //LocalDecl// | -> | //Decl//
  | //[LocalDecl]// | -> | **eps**
  |  |  **|**  | //LocalDecl// ``;`` //[LocalDecl]//
  | //Annotation// | -> | ``inline``
  | //[Annotation]// | -> | **eps**
  |  |  **|**  | //Annotation// //[Annotation]//
  | //ParamDecl// | -> | //StellaIdent// ``:`` //Type//
  | //[ParamDecl]// | -> | **eps**
  |  |  **|**  | //ParamDecl//
  |  |  **|**  | //ParamDecl// ``,`` //[ParamDecl]//
  | //ReturnType// | -> | **eps**
  |  |  **|**  | ``->`` //Type//
  | //ThrowType// | -> | **eps**
  |  |  **|**  | ``throws`` //[Type9]//
  | //Type9// | -> | //Type//
  | //[Type9]// | -> | //Type9//
  |  |  **|**  | //Type9// ``,`` //[Type9]//
  | //MatchCase// | -> | //Pattern// ``=>`` //Expr//
  | //[MatchCase]// | -> | **eps**
  |  |  **|**  | //MatchCase//
  |  |  **|**  | //MatchCase// ``|`` //[MatchCase]//
  | //OptionalTyping// | -> | **eps**
  |  |  **|**  | ``:`` //Type//
  | //PatternData// | -> | **eps**
  |  |  **|**  | ``=`` //Pattern//
  | //ExprData// | -> | **eps**
  |  |  **|**  | ``=`` //Expr//
  | //Pattern// | -> | ``<|`` //StellaIdent// //PatternData// ``|>``
  |  |  **|**  | ``inl`` ``(`` //Pattern// ``)``
  |  |  **|**  | ``inr`` ``(`` //Pattern// ``)``
  |  |  **|**  | ``{`` //[Pattern]// ``}``
  |  |  **|**  | ``{`` //[LabelledPattern]// ``}``
  |  |  **|**  | ``[`` //[Pattern]// ``]``
  |  |  **|**  | ``(`` //Pattern// ``,`` //Pattern// ``)``
  |  |  **|**  | ``false``
  |  |  **|**  | ``true``
  |  |  **|**  | ``unit``
  |  |  **|**  | //Integer//
  |  |  **|**  | ``succ`` ``(`` //Pattern// ``)``
  |  |  **|**  | //StellaIdent//
  |  |  **|**  | ``(`` //Pattern// ``)``
  | //[Pattern]// | -> | **eps**
  |  |  **|**  | //Pattern//
  |  |  **|**  | //Pattern// ``,`` //[Pattern]//
  | //LabelledPattern// | -> | //StellaIdent// ``=`` //Pattern//
  | //[LabelledPattern]// | -> | //LabelledPattern//
  |  |  **|**  | //LabelledPattern// ``,`` //[LabelledPattern]//
  | //Binding// | -> | //StellaIdent// ``=`` //Expr//
  | //[Binding]// | -> | //Binding//
  |  |  **|**  | //Binding// ``,`` //[Binding]//
  | //Expr// | -> | //Expr1// ``;`` //Expr//
  |  |  **|**  | //Expr1// ``;``
  |  |  **|**  | ``let`` //[PatternBinding]// ``in`` //Expr//
  |  |  **|**  | ``letrec`` //[PatternBinding]// ``in`` //Expr//
  |  |  **|**  | ``generic`` ``[`` //[StellaIdent]// ``]`` //Expr//
  |  |  **|**  | //Expr1//
  | //[Expr]// | -> | **eps**
  |  |  **|**  | //Expr//
  |  |  **|**  | //Expr// ``,`` //[Expr]//
  | //Expr1// | -> | //Expr2// ``:=`` //Expr1//
  |  |  **|**  | ``if`` //Expr1// ``then`` //Expr1// ``else`` //Expr1//
  |  |  **|**  | //Expr2//
  | //PatternBinding// | -> | //Pattern// ``=`` //Expr//
  | //[PatternBinding]// | -> | //PatternBinding//
  |  |  **|**  | //PatternBinding// ``,`` //[PatternBinding]//
  | //Expr2// | -> | //Expr3// ``<`` //Expr3//
  |  |  **|**  | //Expr3// ``<=`` //Expr3//
  |  |  **|**  | //Expr3// ``>`` //Expr3//
  |  |  **|**  | //Expr3// ``>=`` //Expr3//
  |  |  **|**  | //Expr3// ``==`` //Expr3//
  |  |  **|**  | //Expr3// ``!=`` //Expr3//
  |  |  **|**  | //Expr3//
  | //[Expr2]// | -> | //Expr2// ``;``
  |  |  **|**  | //Expr2// ``;`` //[Expr2]//
  | //Expr3// | -> | //Expr3// ``as`` //Type2//
  |  |  **|**  | //Expr3// ``cast`` ``as`` //Type2//
  |  |  **|**  | ``fn`` ``(`` //[ParamDecl]// ``)`` ``{`` ``return`` //Expr// ``}``
  |  |  **|**  | ``<|`` //StellaIdent// //ExprData// ``|>``
  |  |  **|**  | ``match`` //Expr2// ``{`` //[MatchCase]// ``}``
  |  |  **|**  | ``[`` //[Expr]// ``]``
  |  |  **|**  | //Expr3// ``+`` //Expr4//
  |  |  **|**  | //Expr3// ``-`` //Expr4//
  |  |  **|**  | //Expr3// ``or`` //Expr4//
  |  |  **|**  | //Expr4//
  | //Expr4// | -> | //Expr4// ``*`` //Expr5//
  |  |  **|**  | //Expr4// ``/`` //Expr5//
  |  |  **|**  | //Expr4// ``and`` //Expr5//
  |  |  **|**  | //Expr5//
  | //Expr5// | -> | ``new`` ``(`` //Expr5// ``)``
  |  |  **|**  | ``*`` //Expr5//
  |  |  **|**  | //Expr6//
  | //Expr6// | -> | //Expr6// ``(`` //[Expr]// ``)``
  |  |  **|**  | //Expr6// ``[`` //[Type]// ``]``
  |  |  **|**  | //Expr6// ``.`` //StellaIdent//
  |  |  **|**  | //Expr6// ``.`` //Integer//
  |  |  **|**  | ``{`` //[Expr]// ``}``
  |  |  **|**  | ``{`` //[Binding]// ``}``
  |  |  **|**  | ``cons`` ``(`` //Expr// ``,`` //Expr// ``)``
  |  |  **|**  | ``List::head`` ``(`` //Expr// ``)``
  |  |  **|**  | ``List::isempty`` ``(`` //Expr// ``)``
  |  |  **|**  | ``List::tail`` ``(`` //Expr// ``)``
  |  |  **|**  | ``panic!``
  |  |  **|**  | ``throw`` ``(`` //Expr// ``)``
  |  |  **|**  | ``try`` ``{`` //Expr// ``}`` ``catch`` ``{`` //Pattern// ``=>`` //Expr// ``}``
  |  |  **|**  | ``try`` ``{`` //Expr// ``}`` ``with`` ``{`` //Expr// ``}``
  |  |  **|**  | ``inl`` ``(`` //Expr// ``)``
  |  |  **|**  | ``inr`` ``(`` //Expr// ``)``
  |  |  **|**  | ``succ`` ``(`` //Expr// ``)``
  |  |  **|**  | ``not`` ``(`` //Expr// ``)``
  |  |  **|**  | ``Nat::pred`` ``(`` //Expr// ``)``
  |  |  **|**  | ``Nat::iszero`` ``(`` //Expr// ``)``
  |  |  **|**  | ``fix`` ``(`` //Expr// ``)``
  |  |  **|**  | ``Nat::rec`` ``(`` //Expr// ``,`` //Expr// ``,`` //Expr// ``)``
  |  |  **|**  | ``fold`` ``[`` //Type// ``]`` //Expr7//
  |  |  **|**  | ``unfold`` ``[`` //Type// ``]`` //Expr7//
  |  |  **|**  | //Expr7//
  | //Expr7// | -> | ``true``
  |  |  **|**  | ``false``
  |  |  **|**  | ``unit``
  |  |  **|**  | //Integer//
  |  |  **|**  | //MemoryAddress//
  |  |  **|**  | //StellaIdent//
  |  |  **|**  | ``(`` //Expr// ``)``
  | //Type// | -> | ``fn`` ``(`` //[Type]// ``)`` ``->`` //Type//
  |  |  **|**  | ``forall`` //[StellaIdent]// ``.`` //Type//
  |  |  **|**  | ``µ`` //StellaIdent// ``.`` //Type//
  |  |  **|**  | //Type1//
  | //Type1// | -> | //Type2// ``+`` //Type2//
  |  |  **|**  | //Type2//
  | //Type2// | -> | ``{`` //[Type]// ``}``
  |  |  **|**  | ``{`` //[RecordFieldType]// ``}``
  |  |  **|**  | ``<|`` //[VariantFieldType]// ``|>``
  |  |  **|**  | ``[`` //Type// ``]``
  |  |  **|**  | //Type3//
  | //Type3// | -> | ``Bool``
  |  |  **|**  | ``Nat``
  |  |  **|**  | ``Unit``
  |  |  **|**  | ``Top``
  |  |  **|**  | ``Bot``
  |  |  **|**  | ``&`` //Type2//
  |  |  **|**  | //StellaIdent//
  |  |  **|**  | ``(`` //Type// ``)``
  | //[Type]// | -> | **eps**
  |  |  **|**  | //Type//
  |  |  **|**  | //Type// ``,`` //[Type]//
  | //VariantFieldType// | -> | //StellaIdent// //OptionalTyping//
  | //[VariantFieldType]// | -> | **eps**
  |  |  **|**  | //VariantFieldType//
  |  |  **|**  | //VariantFieldType// ``,`` //[VariantFieldType]//
  | //RecordFieldType// | -> | //StellaIdent// ``:`` //Type//
  | //[RecordFieldType]// | -> | //RecordFieldType//
  |  |  **|**  | //RecordFieldType// ``,`` //[RecordFieldType]//
  | //Typing// | -> | //Expr// ``:`` //Type//



%% File generated by the BNF Converter (bnfc 2.9.5).
